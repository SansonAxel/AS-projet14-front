import PropTypes from 'prop-types';
import './CreateForm.scss';

const CreateForm = () => {
  return <div className="CreateForm">CreateForm</div>;
};

CreateForm.propTypes = {};
export default CreateForm;
